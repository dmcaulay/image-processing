
var async = require('async')
var assert = require('assert')
var fs = require('fs')
var unoptimize = require('../../../../lib/imageProcessing/processors/unoptimize')
var path = require('path')

var quip = {
  id: 'test',
  files: {}
}

describe('unoptimize', function() {
  // it('handles animated images', function(done) {
  //   quip.files.original = path.join(__dirname, '../../../fixtures/images/animated.gif')
  //   unoptimize(quip, function(err) {
  //     assert.ifError(err)
  //     done()
  //   })
  // })
})

